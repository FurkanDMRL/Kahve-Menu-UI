import 'package:flutter/material.dart';

class Alertsayfasi extends StatelessWidget {
  const Alertsayfasi({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Alert Sayfası'),);
  }
}